/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { LastPunchTimeDto } from '../../models/last-punch-time-dto';

export interface GetLastPunchTime$Params {
}

export function getLastPunchTime(http: HttpClient, rootUrl: string, params?: GetLastPunchTime$Params, context?: HttpContext): Observable<StrictHttpResponse<LastPunchTimeDto>> {
  const rb = new RequestBuilder(rootUrl, getLastPunchTime.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LastPunchTimeDto>;
    })
  );
}

getLastPunchTime.PATH = '/attendance/last-punch';
