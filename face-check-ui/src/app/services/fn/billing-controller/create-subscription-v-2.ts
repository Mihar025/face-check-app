/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BillingResponse } from '../../models/billing-response';
import { CreateSubscriptionRequest } from '../../models/create-subscription-request';

export interface CreateSubscriptionV2$Params {
      body: CreateSubscriptionRequest
}

export function createSubscriptionV2(http: HttpClient, rootUrl: string, params: CreateSubscriptionV2$Params, context?: HttpContext): Observable<StrictHttpResponse<BillingResponse>> {
  const rb = new RequestBuilder(rootUrl, createSubscriptionV2.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BillingResponse>;
    })
  );
}

createSubscriptionV2.PATH = '/billing/activate/subscription';
