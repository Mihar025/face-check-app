/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponseRelatedUserInCompanyResponse } from '../../models/page-response-related-user-in-company-response';

export interface FindAllEmployees$Params {
  page?: number;
  size?: number;
}

export function findAllEmployees(http: HttpClient, rootUrl: string, params?: FindAllEmployees$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponseRelatedUserInCompanyResponse>> {
  const rb = new RequestBuilder(rootUrl, findAllEmployees.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponseRelatedUserInCompanyResponse>;
    })
  );
}

findAllEmployees.PATH = '/company/find-all-employees';
