/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { EmployeeRaiseHourRateRequest } from '../../models/employee-raise-hour-rate-request';
import { EmployeeSalaryResponse } from '../../models/employee-salary-response';

export interface UpdateEmployeeRate$Params {
  companyId: number;
  employeeId: number;
      body: EmployeeRaiseHourRateRequest
}

export function updateEmployeeRate(http: HttpClient, rootUrl: string, params: UpdateEmployeeRate$Params, context?: HttpContext): Observable<StrictHttpResponse<EmployeeSalaryResponse>> {
  const rb = new RequestBuilder(rootUrl, updateEmployeeRate.PATH, 'put');
  if (params) {
    rb.path('companyId', params.companyId, {});
    rb.path('employeeId', params.employeeId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<EmployeeSalaryResponse>;
    })
  );
}

updateEmployeeRate.PATH = '/company/{companyId}/employees/{employeeId}/rate';
