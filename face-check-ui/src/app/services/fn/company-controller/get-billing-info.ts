/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CompanyStripeResponse } from '../../models/company-stripe-response';

export interface GetBillingInfo$Params {
  companyId: number;
}

export function getBillingInfo(http: HttpClient, rootUrl: string, params: GetBillingInfo$Params, context?: HttpContext): Observable<StrictHttpResponse<CompanyStripeResponse>> {
  const rb = new RequestBuilder(rootUrl, getBillingInfo.PATH, 'get');
  if (params) {
    rb.path('companyId', params.companyId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CompanyStripeResponse>;
    })
  );
}

getBillingInfo.PATH = '/company/{companyId}/billing-info';
