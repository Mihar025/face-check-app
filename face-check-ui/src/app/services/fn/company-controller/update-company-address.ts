/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UpdateCompanyAddressRequest } from '../../models/update-company-address-request';
import { UpdateCompanyAddressResponse } from '../../models/update-company-address-response';

export interface UpdateCompanyAddress$Params {
      body: UpdateCompanyAddressRequest
}

export function updateCompanyAddress(http: HttpClient, rootUrl: string, params: UpdateCompanyAddress$Params, context?: HttpContext): Observable<StrictHttpResponse<UpdateCompanyAddressResponse>> {
  const rb = new RequestBuilder(rootUrl, updateCompanyAddress.PATH, 'put');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UpdateCompanyAddressResponse>;
    })
  );
}

updateCompanyAddress.PATH = '/company/update-address';
