/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TermsOfUseRequestDataRequest } from '../../models/terms-of-use-request-data-request';
import { TermsOfUseRequestDataResponse } from '../../models/terms-of-use-request-data-response';

export interface AcceptTerms$Params {
      body: TermsOfUseRequestDataRequest
}

export function acceptTerms(http: HttpClient, rootUrl: string, params: AcceptTerms$Params, context?: HttpContext): Observable<StrictHttpResponse<TermsOfUseRequestDataResponse>> {
  const rb = new RequestBuilder(rootUrl, acceptTerms.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TermsOfUseRequestDataResponse>;
    })
  );
}

acceptTerms.PATH = '/privacy-and-terms';
