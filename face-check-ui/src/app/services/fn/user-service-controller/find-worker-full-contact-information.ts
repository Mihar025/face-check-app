/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UserFullContactInformation } from '../../models/user-full-contact-information';

export interface FindWorkerFullContactInformation$Params {
}

export function findWorkerFullContactInformation(http: HttpClient, rootUrl: string, params?: FindWorkerFullContactInformation$Params, context?: HttpContext): Observable<StrictHttpResponse<UserFullContactInformation>> {
  const rb = new RequestBuilder(rootUrl, findWorkerFullContactInformation.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UserFullContactInformation>;
    })
  );
}

findWorkerFullContactInformation.PATH = '/user/contact-info';
