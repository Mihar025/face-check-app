/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReportFileDto } from '../../models/report-file-dto';

export interface GetCompanyReports$Params {
  companyId: number;
  companyName: string;
}

export function getCompanyReports(http: HttpClient, rootUrl: string, params: GetCompanyReports$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ReportFileDto>>> {
  const rb = new RequestBuilder(rootUrl, getCompanyReports.PATH, 'get');
  if (params) {
    rb.path('companyId', params.companyId, {});
    rb.query('companyName', params.companyName, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ReportFileDto>>;
    })
  );
}

getCompanyReports.PATH = '/aws-reports/company/{companyId}';
