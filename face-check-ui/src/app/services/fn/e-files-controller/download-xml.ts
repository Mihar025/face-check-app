/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface DownloadXml$Params {
  companyId: number;
  userId: number;
  year: number;
  quarter: number;
}

export function downloadXml(http: HttpClient, rootUrl: string, params: DownloadXml$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<string>>> {
  const rb = new RequestBuilder(rootUrl, downloadXml.PATH, 'get');
  if (params) {
    rb.path('companyId', params.companyId, {});
    rb.query('userId', params.userId, {});
    rb.query('year', params.year, {});
    rb.query('quarter', params.quarter, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<string>>;
    })
  );
}

downloadXml.PATH = '/e-file/{companyId}/download';
