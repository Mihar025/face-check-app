/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface GenerateXml$Params {
  companyId: number;
  userId: number;
  year: number;
  quarter: number;
}

export function generateXml(http: HttpClient, rootUrl: string, params: GenerateXml$Params, context?: HttpContext): Observable<StrictHttpResponse<string>> {
  const rb = new RequestBuilder(rootUrl, generateXml.PATH, 'get');
  if (params) {
    rb.path('companyId', params.companyId, {});
    rb.query('userId', params.userId, {});
    rb.query('year', params.year, {});
    rb.query('quarter', params.quarter, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<string>;
    })
  );
}

generateXml.PATH = '/e-file/{companyId}/xml';
