/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaymentHistoryResponse } from '../../models/payment-history-response';

export interface GetPaymentByDate$Params {
  date: string;
}

export function getPaymentByDate(http: HttpClient, rootUrl: string, params: GetPaymentByDate$Params, context?: HttpContext): Observable<StrictHttpResponse<PaymentHistoryResponse>> {
  const rb = new RequestBuilder(rootUrl, getPaymentByDate.PATH, 'get');
  if (params) {
    rb.query('date', params.date, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaymentHistoryResponse>;
    })
  );
}

getPaymentByDate.PATH = '/payment-history-irs/by-date';
