/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponsePaymentHistoryResponse } from '../../models/page-response-payment-history-response';

export interface GetPaymentsByQuarter$Params {
  companyId: number;
  year: number;
  quarter: number;
  page?: number;
  size?: number;
}

export function getPaymentsByQuarter(http: HttpClient, rootUrl: string, params: GetPaymentsByQuarter$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponsePaymentHistoryResponse>> {
  const rb = new RequestBuilder(rootUrl, getPaymentsByQuarter.PATH, 'get');
  if (params) {
    rb.query('companyId', params.companyId, {});
    rb.query('year', params.year, {});
    rb.query('quarter', params.quarter, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponsePaymentHistoryResponse>;
    })
  );
}

getPaymentsByQuarter.PATH = '/payment-history-irs/quarter';
