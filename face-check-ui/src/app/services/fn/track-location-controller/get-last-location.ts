/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { LocationRecordDto } from '../../models/location-record-dto';

export interface GetLastLocation$Params {

/**
 * User ID
 */
  userId: number;
}

export function getLastLocation(http: HttpClient, rootUrl: string, params: GetLastLocation$Params, context?: HttpContext): Observable<StrictHttpResponse<LocationRecordDto>> {
  const rb = new RequestBuilder(rootUrl, getLastLocation.PATH, 'get');
  if (params) {
    rb.path('userId', params.userId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LocationRecordDto>;
    })
  );
}

getLastLocation.PATH = '/location/last/{userId}';
