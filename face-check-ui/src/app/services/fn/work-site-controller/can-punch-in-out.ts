/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CanPunchOutRequestWorkSite } from '../../models/can-punch-out-request-work-site';

export interface CanPunchInOut$Params {
  workSiteId: number;
  userId: number;
      body: CanPunchOutRequestWorkSite
}

export function canPunchInOut(http: HttpClient, rootUrl: string, params: CanPunchInOut$Params, context?: HttpContext): Observable<StrictHttpResponse<boolean>> {
  const rb = new RequestBuilder(rootUrl, canPunchInOut.PATH, 'post');
  if (params) {
    rb.path('workSiteId', params.workSiteId, {});
    rb.path('userId', params.userId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: String((r as HttpResponse<any>).body) === 'true' }) as StrictHttpResponse<boolean>;
    })
  );
}

canPunchInOut.PATH = '/workSite/{workSiteId}/can-punch/{userId}';
