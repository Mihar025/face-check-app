/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WorkSiteUpdateWorkingHoursRequest } from '../../models/work-site-update-working-hours-request';
import { WorkSiteUpdateWorkingHoursResponse } from '../../models/work-site-update-working-hours-response';

export interface UpdateWorkingHours$Params {
  workSiteId: number;
      body: WorkSiteUpdateWorkingHoursRequest
}

export function updateWorkingHours(http: HttpClient, rootUrl: string, params: UpdateWorkingHours$Params, context?: HttpContext): Observable<StrictHttpResponse<WorkSiteUpdateWorkingHoursResponse>> {
  const rb = new RequestBuilder(rootUrl, updateWorkingHours.PATH, 'patch');
  if (params) {
    rb.path('workSiteId', params.workSiteId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WorkSiteUpdateWorkingHoursResponse>;
    })
  );
}

updateWorkingHours.PATH = '/workSite/{workSiteId}/working-hours';
