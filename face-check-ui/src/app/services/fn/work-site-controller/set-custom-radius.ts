/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SetNewCustomRadiusRequest } from '../../models/set-new-custom-radius-request';
import { SetNewCustomRadiusResponse } from '../../models/set-new-custom-radius-response';

export interface SetCustomRadius$Params {
  workSiteId: number;
      body: SetNewCustomRadiusRequest
}

export function setCustomRadius(http: HttpClient, rootUrl: string, params: SetCustomRadius$Params, context?: HttpContext): Observable<StrictHttpResponse<SetNewCustomRadiusResponse>> {
  const rb = new RequestBuilder(rootUrl, setCustomRadius.PATH, 'post');
  if (params) {
    rb.path('workSiteId', params.workSiteId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SetNewCustomRadiusResponse>;
    })
  );
}

setCustomRadius.PATH = '/workSite/{workSiteId}/custom-radius';
