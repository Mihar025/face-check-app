/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface GenerateForm1095CPdf$Params {
  userId: number;
  companyId: number;
  reportYear: number;
}

export function generateForm1095CPdf(http: HttpClient, rootUrl: string, params: GenerateForm1095CPdf$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<string>>> {
  const rb = new RequestBuilder(rootUrl, generateForm1095CPdf.PATH, 'get');
  if (params) {
    rb.path('userId', params.userId, {});
    rb.path('companyId', params.companyId, {});
    rb.path('reportYear', params.reportYear, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<string>>;
    })
  );
}

generateForm1095CPdf.PATH = '/taxes-forms/generate1095c_pdf/{companyId}/{userId}/{reportYear}';
