/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PunchInUpdateRequest } from '../../models/punch-in-update-request';
import { UpdatePunchInForWorkerResponse } from '../../models/update-punch-in-for-worker-response';

export interface UpdatePunchInTime$Params {
  workerId: number;
      body: PunchInUpdateRequest
}

export function updatePunchInTime(http: HttpClient, rootUrl: string, params: UpdatePunchInTime$Params, context?: HttpContext): Observable<StrictHttpResponse<UpdatePunchInForWorkerResponse>> {
  const rb = new RequestBuilder(rootUrl, updatePunchInTime.PATH, 'put');
  if (params) {
    rb.path('workerId', params.workerId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UpdatePunchInForWorkerResponse>;
    })
  );
}

updatePunchInTime.PATH = '/admin/worker/{workerId}/punch-in';
