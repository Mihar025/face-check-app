/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RandonAttendanceVerificationRequest } from '../../models/randon-attendance-verification-request';
import { RandonAttendanceVerificationResponse } from '../../models/randon-attendance-verification-response';

export interface CompleteRandomAttendanceVerification$Params {
      body: RandonAttendanceVerificationRequest
}

export function completeRandomAttendanceVerification(http: HttpClient, rootUrl: string, params: CompleteRandomAttendanceVerification$Params, context?: HttpContext): Observable<StrictHttpResponse<RandonAttendanceVerificationResponse>> {
  const rb = new RequestBuilder(rootUrl, completeRandomAttendanceVerification.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RandonAttendanceVerificationResponse>;
    })
  );
}

completeRandomAttendanceVerification.PATH = '/remote-worker/random-attendance-verification';
