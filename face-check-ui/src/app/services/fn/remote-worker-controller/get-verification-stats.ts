/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RemoteVerificationStatsResponse } from '../../models/remote-verification-stats-response';

export interface GetVerificationStats$Params {
  companyId?: number;
}

export function getVerificationStats(http: HttpClient, rootUrl: string, params?: GetVerificationStats$Params, context?: HttpContext): Observable<StrictHttpResponse<RemoteVerificationStatsResponse>> {
  const rb = new RequestBuilder(rootUrl, getVerificationStats.PATH, 'get');
  if (params) {
    rb.query('companyId', params.companyId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RemoteVerificationStatsResponse>;
    })
  );
}

getVerificationStats.PATH = '/remote-worker/admin/verifications/stats';
