/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponseRemoteVerificationAdminResponse } from '../../models/page-response-remote-verification-admin-response';

export interface GetAllVerifications$Params {
  companyId?: number;
  status?: string;
  dateFrom?: string;
  dateTo?: string;
  page?: number;
  size?: number;
}

export function getAllVerifications(http: HttpClient, rootUrl: string, params?: GetAllVerifications$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponseRemoteVerificationAdminResponse>> {
  const rb = new RequestBuilder(rootUrl, getAllVerifications.PATH, 'get');
  if (params) {
    rb.query('companyId', params.companyId, {});
    rb.query('status', params.status, {});
    rb.query('dateFrom', params.dateFrom, {});
    rb.query('dateTo', params.dateTo, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponseRemoteVerificationAdminResponse>;
    })
  );
}

getAllVerifications.PATH = '/remote-worker/admin/verifications';
