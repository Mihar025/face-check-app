name: api_client
version: 1.0.0
description: Generated API Client
homepage: homepage

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dio: ^5.4.0
  one_of: ^1.5.0
  one_of_serializer: ^1.5.0
  built_value: ^8.9.0
  built_collection: ^5.1.1
  json_annotation: ^4.8.1
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  built_value_generator: ^8.9.0
  build_runner: ^2.4.7
  analyzer: ^7.1.0
  json_serializable: ^6.7.1
  lints: ^3.0.0
  test: ^1.24.0

# Удаляем chopper_generator, так как он создает конфликты

# Добавляем flutter секцию если её нет
flutter:
  uses-material-design: true